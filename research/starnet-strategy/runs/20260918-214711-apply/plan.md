# Apply 计划
根据decision将候选原型审阅整理为src/starnet，新增屏蔽单步授权与公开邻居更新；保留校准初始反馈。
打包后必须直接从ZIP独立解压运行真实CaseVO+LLM。先种子41（10节点、30预算），通过再42（50节点、100预算）和43（100节点、200预算）。
使用现有外部venv作为只读完整CaseVO运行时，uv run --no-project --python指定解释器；不修改其源码/依赖。轻量测试仍用本仓库锁定Python3.9。
每次实验均run_limited串行；本地LLM请求连接5s/读取45s、0自动重试，模型55s截止；环境HTTP连接5s/读取20s、写请求不重试。
