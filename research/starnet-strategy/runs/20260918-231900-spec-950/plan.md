# Prepare / Specify: official score target 950

User reports incumbent starnet-20260918.zip scored 531.1233, leaderboard top1
948.58. Target 950 requires +78.8662% against incumbent. These are user-reported
platform results, not independently retrieved. Preserve incumbent ZIP and history.

First measure influence basis on small official sandbox custom graphs, then
compare topology-aware marginal intervention against incumbent degree proxies.
Only documented public APIs are allowed. Participant never receives seed r,
prompt strengths, natural score, or evaluator access.

Matrix: path, star, cycle, complete, disconnected, preferential attachment,
small-world, random, community graphs; 50-node budget100 primary; 100-node
budget200 smoke; varied signs, persona correlations, response heterogeneity,
all six prompt permutations and weak/negative effects. Separate development and
held-out indices. Keep identical JSON per paired run and SHA256 provenance.

Metrics: final/natural score, paired delta, win count, downside, budget, steps,
LLM calls, HTTP calls, wall time, RSS. Failed sessions excluded explicitly;
mutations never retried. Small tests first, experiments serial via run_limited.sh.

Gate: validate surrogate on unseen graphs before use; do not infer exact official
formula from a fit. Algorithm controls cannot substitute for LLM-authorized ZIP
tests. Apply only evidence-supported changes and preserve regressions. Official
hidden mean >=950 remains the acceptance gate, requiring a new platform result.
