# Prepare / Specify results

Read task, supplements, FAQ, existing source, scheme, experience, research history.
Incumbent scans all nodes, tests prompts on one node, uses degree+1 communication
weight and negative burden for shields. Candidate truncation can show three
prompts for each target, reducing target diversity. No marginal topology score.
Existing comparison: only three 50-node selection seeds; prior real-LLM runs
include one 50-node case. A broader paired matrix is required.

Environment: locked Python3.9 lightweight dependencies already synchronized.
No external casevo changes. No secrets printed. Next: Baseline and Explore.
