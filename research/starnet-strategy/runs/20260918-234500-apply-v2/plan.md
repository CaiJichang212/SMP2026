# Apply V2

Scope from decision.md: topology.py, model cut branch, structural plan validation,
commander/config, deterministic builder, targeted tests, docs and benchmark files.
Preserve originalZIP. Final ZIP uses only policy/topology/model and prompt/config;
research harnesses, credentials and test seeds excluded.

Convert measured prototype to purePython3.9-compatible module, keep nominal
candidate ordering. Expose stress metric without hard rejection. Test candidate
ordering equivalence, normalized components, isolates, legal cut, stale batch
rejection, actual authorized cut routing, and original contracts.
