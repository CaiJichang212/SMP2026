# SMP 2026 星网项目协作规范

修改智能体前，先阅读 `docs/task/赛题任务.md`、`docs/task/补充信息.md` 和常见问题解答。
本地存在 `.agents/skills/research-lab/SKILL.md` 时，按其中流程在
`research/starnet-strategy/` 记录各研究阶段。实验应记录可比较的种子、日志、
大模型调用数、步数、预算和最终得分；
区分模拟环境、本地测试与官方隐藏种子平台的结果。导入成功、静态检查或单个样例种子
均不能证明线上有效。

## 目录与合规边界

- 验证替代方案前，保留 `SMP_Starter_Kit/` 作为参考 SDK。新编写的可复用 Python
  代码放在 `src/starnet/`，测试数据放在 `data/`，测试放在 `tests/`，脚本放在
  `scripts/`，生成的 ZIP 包放在 `submissions/`。
- 提交 ZIP 的根目录只能包含 `config.json`、`prompt/` 和 `starnet_model.py`；
  保持 `ParticipantSquadModel` 类名及所需接口签名。不得打包 `.env`、测试数据、
  原始实验输出或本地依赖。
- 遵守赛题规则：使用 CaseVO/agent_mesa 的 `AgentBase`，让大模型真正参与核心决策；
  不得读取环境隐藏内部状态、通过反射绕过代理或调用规定之外的环境接口。
- 本地 `casevo` 仓库位于本项目之外，未经授权不要修改。其导入名与线上旧版
  `agent_mesa` 不同；调整源码前，先在受限测试中验证兼容性。
- 不打印、提交、粘贴或发送 API 密钥。本地凭证仅放在已忽略的根目录 `.env`，
  不得写入模型代码或提交 ZIP。

## 环境与资源控制

- 使用 `UV_CACHE_DIR=/tmp/smp20260918-uv-cache uv sync --locked` 和 `uv run`，
  依赖版本以仓库中的 `uv.lock` 为准，默认 Python 3.9。轻量图算法测试不必安装
  完整 CaseVO/Chroma 依赖；核对线上 Python 及依赖列表后再单独记录运行时需求。
- 这台主机为 2 vCPU、约 4 GB 内存。耗资源实验须通过 `scripts/run_limited.sh`
  串行运行（单核、限制线程数、最长 10 分钟、虚拟地址空间上限 2.5 GiB）。
  先用小种子和较少调用量测试；若出现频繁换页、内存压力或服务故障，停止并记录原因。
- 不直接运行未经修改的 `SMP_Starter_Kit/local_test.py`：其中有示例密钥占位符、
  过时的 50 步限制，远端请求也没有超时。远端调用必须设置超时、有限重试并记录来源。
- 步数和大模型调用数分别受限：初赛各 120 次，复赛各 250 次；不能依赖旧测试脚本的
  50 步占位限制。每次调用前检查预算及动作有效性。
- 如果本地可行方案均已尝试，且继续推进确实需要用户协助，给 `li_y_c@qq.com`
  发送简短邮件，说明现象、证据、已尝试的解决办法和所需操作。邮件不得包含凭证；
  日常进度无需发邮件。
